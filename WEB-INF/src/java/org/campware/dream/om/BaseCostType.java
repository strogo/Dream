package org.campware.dream.om;


import java.math.BigDecimal;
import java.sql.Connection;
import java.util.ArrayList;
import java.util.Date;
import java.util.Collections;
import java.util.List;

import org.apache.commons.lang.ObjectUtils;
import org.apache.torque.TorqueException;
import org.apache.torque.om.BaseObject;
import org.apache.torque.om.ComboKey;
import org.apache.torque.om.DateKey;
import org.apache.torque.om.NumberKey;
import org.apache.torque.om.ObjectKey;
import org.apache.torque.om.SimpleKey;
import org.apache.torque.om.StringKey;
import org.apache.torque.om.Persistent;
import org.apache.torque.util.Criteria;
import org.apache.torque.util.Transaction;


/**
 * This class was autogenerated by Torque on:
 *
 * [Thu Jul 29 08:38:49 CEST 2004]
 *
 * You should not use this class directly.  It should not even be
 * extended all references should be to CostType
 */
public abstract class BaseCostType extends BaseObject
{
    /** The Peer class */
    private static final CostTypePeer peer =
        new CostTypePeer();

                  
        /**
         * The value for the cost_type_id field
         */
        private int cost_type_id;
                                                                            
        /**
         * The value for the cost_type_function field
         */
        private int cost_type_function = 10;
              
        /**
         * The value for the cost_type_name field
         */
        private String cost_type_name;
      
      
        /**
         * Get the CostTypeId
         *
         * @return int
         */
        public int getCostTypeId()
        {
            return cost_type_id;
        }

                                                                              
        /**
         * Set the value of CostTypeId
         *
         * @param v new value
         */
        public void setCostTypeId(int v) throws TorqueException
        {
          


         if (this.cost_type_id != v)
        {
             this.cost_type_id = v;
            setModified(true);
        }

                  
                                                  
              // update associated DcontractItem
              if (collDcontractItems != null)
              {
                  for (int i = 0; i < collDcontractItems.size(); i++)
                  {
                      ((DcontractItem) collDcontractItems.get(i))
                          .setCostTypeId(v);
                  }
              }
                                   }


        /**
         * Get the CostTypeFunction
         *
         * @return int
         */
        public int getCostTypeFunction()
        {
            return cost_type_function;
        }

                                            
        /**
         * Set the value of CostTypeFunction
         *
         * @param v new value
         */
        public void setCostTypeFunction(int v) 
        {
          


         if (this.cost_type_function != v)
        {
             this.cost_type_function = v;
            setModified(true);
        }

                  
                       }


        /**
         * Get the CostTypeName
         *
         * @return String
         */
        public String getCostTypeName()
        {
            return cost_type_name;
        }

                                            
        /**
         * Set the value of CostTypeName
         *
         * @param v new value
         */
        public void setCostTypeName(String v) 
        {
          


         if (!ObjectUtils.equals(this.cost_type_name, v))
        {
             this.cost_type_name = v;
            setModified(true);
        }

                  
                       }


 
        
                
      
    /**
     * Collection to store aggregation of collDcontractItems
     */
    protected List collDcontractItems;

    /**
     * Temporary storage of collDcontractItems to save a possible db hit in
     * the event objects are add to the collection, but the
     * complete collection is never requested.
     */
    protected void initDcontractItems()
    {
        if (collDcontractItems == null)
        {
            collDcontractItems = new ArrayList();
        }
    }

    /**
     * Method called to associate a DcontractItem object to this object
     * through the DcontractItem foreign key attribute
     *
     * @param l DcontractItem
     * @throws TorqueException
     */
    public void addDcontractItem(DcontractItem l) throws TorqueException
    {
        getDcontractItems().add(l);
        l.setCostType((CostType) this);
    }

    /**
     * The criteria used to select the current contents of collDcontractItems
     */
    private Criteria lastDcontractItemsCriteria = null;

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getDcontractItems(new Criteria())
     *
     * @throws TorqueException
     */
    public List getDcontractItems() throws TorqueException
    {
        if (collDcontractItems == null)
        {
            collDcontractItems = getDcontractItems(new Criteria(10));
        }
        return collDcontractItems;
    }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this CostType has previously
     * been saved, it will retrieve related DcontractItems from storage.
     * If this CostType is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     *
     * @throws TorqueException
     */
    public List getDcontractItems(Criteria criteria) throws TorqueException
    {
        if (collDcontractItems == null)
        {
            if (isNew())
            {
               collDcontractItems = new ArrayList();
            }
            else
            {
                   criteria.add(DcontractItemPeer.COST_TYPE_ID, getCostTypeId() );
                   collDcontractItems = DcontractItemPeer.doSelect(criteria);
            }
        }
        else
        {
            // criteria has no effect for a new object
            if (!isNew())
            {
                // the following code is to determine if a new query is
                // called for.  If the criteria is the same as the last
                // one, just return the collection.
                   criteria.add(DcontractItemPeer.COST_TYPE_ID, getCostTypeId());
                   if (!lastDcontractItemsCriteria.equals(criteria))
                {
                    collDcontractItems = DcontractItemPeer.doSelect(criteria);
                }
            }
        }
        lastDcontractItemsCriteria = criteria;

        return collDcontractItems;
    }

    /**
     * If this collection has already been initialized, returns
     * the collection. Otherwise returns the results of
     * getDcontractItems(new Criteria(),Connection)
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getDcontractItems(Connection con) throws TorqueException
    {
        if (collDcontractItems == null)
        {
            collDcontractItems = getDcontractItems(new Criteria(10), con);
        }
        return collDcontractItems;
    }

    /**
     * If this collection has already been initialized with
     * an identical criteria, it returns the collection.
     * Otherwise if this CostType has previously
     * been saved, it will retrieve related DcontractItems from storage.
     * If this CostType is new, it will return
     * an empty collection or the current collection, the criteria
     * is ignored on a new object.
     * This method takes in the Connection also as input so that
     * referenced objects can also be obtained using a Connection
     * that is taken as input
     */
    public List getDcontractItems(Criteria criteria, Connection con)
            throws TorqueException
    {
        if (collDcontractItems == null)
        {
            if (isNew())
            {
               collDcontractItems = new ArrayList();
            }
            else
            {
                     criteria.add(DcontractItemPeer.COST_TYPE_ID, getCostTypeId());
                     collDcontractItems = DcontractItemPeer.doSelect(criteria, con);
             }
         }
         else
         {
             // criteria has no effect for a new object
             if (!isNew())
             {
                 // the following code is to determine if a new query is
                 // called for.  If the criteria is the same as the last
                 // one, just return the collection.
                     criteria.add(DcontractItemPeer.COST_TYPE_ID, getCostTypeId());
                     if (!lastDcontractItemsCriteria.equals(criteria))
                 {
                     collDcontractItems = DcontractItemPeer.doSelect(criteria, con);
                 }
             }
         }
         lastDcontractItemsCriteria = criteria;

         return collDcontractItems;
     }

    

     
      
      
          
                    
                
        
        
    
      
         
          
                    
                
        
        
      



     
    
     


    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     *
     * @throws Exception
     */
    public void save() throws Exception
    {
             save(CostTypePeer.getMapBuilder()
                .getDatabaseMap().getName());
     }

    /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.
     * Note: this code is here because the method body is
     * auto-generated conditionally and therefore needs to be
     * in this file instead of in the super class, BaseObject.
     *
     * @param dbName
     * @throws TorqueException
     */
    public void save(String dbName) throws TorqueException
    {
        Connection con = null;
         try
        {
            con = Transaction.begin(dbName);
            save(con);
            Transaction.commit(con);
        }
        catch(TorqueException e)
        {
            Transaction.safeRollback(con);
            throw e;
        }

     }

      /** flag to prevent endless save loop, if this object is referenced
        by another object which falls in this transaction. */
    private boolean alreadyInSave = false;
      /**
     * Stores the object in the database.  If the object is new,
     * it inserts it; otherwise an update is performed.  This method
     * is meant to be used as part of a transaction, otherwise use
     * the save() method and the connection details will be handled
     * internally
     *
     * @param con
     * @throws TorqueException
     */
    public void save(Connection con) throws TorqueException
    {
        if (!alreadyInSave)
      {
        alreadyInSave = true;



  
        // If this object has been modified, then save it to the database.
        if (isModified())
        {
            if (isNew())
            {
                CostTypePeer.doInsert((CostType) this, con);
                setNew(false);
            }
            else
            {
                CostTypePeer.doUpdate((CostType) this, con);
            }
        }

                                    
                
          if (collDcontractItems != null)
          {
              for (int i = 0; i < collDcontractItems.size(); i++)
              {
                  ((DcontractItem) collDcontractItems.get(i)).save(con);
              }
          }
                  alreadyInSave = false;
      }
      }


                        
    
    

        /**
     * Set the PrimaryKey using ObjectKey.
     *
     * @param  cost_type_id ObjectKey
     */
    public void setPrimaryKey(ObjectKey key)
        throws TorqueException
    {
                    setCostTypeId(((NumberKey) key).intValue());
            }

    /**
     * Set the PrimaryKey using a String.
     *
     * @param key
     */
    public void setPrimaryKey(String key) throws TorqueException
    {
                    setCostTypeId(Integer.parseInt(key));
            }


    /**
     * returns an id that differentiates this object from others
     * of its class.
     */
    public ObjectKey getPrimaryKey()
    {
        return SimpleKey.keyFor(getCostTypeId());
    }

 

    /**
     * Makes a copy of this object.
     * It creates a new object filling in the simple attributes.
     * It then fills all the association collections and sets the
     * related objects to isNew=true.
     */
    public CostType copy() throws TorqueException
    {
        return copyInto(new CostType());
    }

    protected CostType copyInto(CostType copyObj) throws TorqueException
    {
        copyObj.setCostTypeId(cost_type_id);
        copyObj.setCostTypeFunction(cost_type_function);
        copyObj.setCostTypeName(cost_type_name);

  copyObj.setNew(false);
                                    
                
        List v = getDcontractItems();
        for (int i = 0; i < v.size(); i++)
        {
            DcontractItem obj = (DcontractItem) v.get(i);
            copyObj.addDcontractItem(obj.copy());
            ((Persistent) v.get(i)).setNew(true);
        }
                copyObj.setNew(true);

                      copyObj.setCostTypeId(0);
                            return copyObj;
    }

    /**
     * returns a peer instance associated with this om.  Since Peer classes
     * are not to have any instance attributes, this method returns the
     * same instance for all member of this class. The method could therefore
     * be static, but this would prevent one from overriding the behavior.
     */
    public CostTypePeer getPeer()
    {
        return peer;
    }
}
